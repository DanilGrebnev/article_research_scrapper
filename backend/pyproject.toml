[project]
name = "scrapper-backend"
version = "0.1.0"
description = "Web scraper backend powered by FastAPI + Selenium"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.14.3",
    "fastapi",
    "requests>=2.32.5",
    "selenium>=4.41.0",
    "uvicorn[standard]",
]
